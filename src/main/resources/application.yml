server:
  port: 8080

spring:
  application:
    name: ai-capability-backend

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level [%thread] [%X{traceId}] %logger - %msg%n"

springdoc:
  swagger-ui:
    path: /swagger-ui.html
